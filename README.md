# Sticky Notes

A simple sticky notes application built with Tkinter. This application allows users to create a quick sticky note with the current time and a custom message.

## Features

- Display the current time on each sticky note.
- Input and display a custom note.
- Simple, clean, and easy-to-use interface with Tkinter.